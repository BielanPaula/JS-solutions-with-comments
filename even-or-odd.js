function even_or_odd(number) {
  if (number % 2 == 0){  //if remainder will be equal 0 (the number is even when it's commensurate by 2)
    return "Even"        //then return even
  }
  else {                 //if remainder will not be equal 0
  return "Odd"           //then return odd
  }
}
